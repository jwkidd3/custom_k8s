apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Student access is granted via the EKS access entry (AmazonEKSClusterAdminPolicy)
# in terraform/modules/eks — no in-cluster student RBAC is needed.
resources:
  - platform.yaml
